= Disk:
== HD speed:
  $ hdparm -t /dev/sda

== Space Used:
  $ df -h       show list of partitions mounted
  $ ls -lh(R)   show size of the files and directories ordered by size
  $ du -sh dir  estimate space used by directory 'dir'

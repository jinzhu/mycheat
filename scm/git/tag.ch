== Git Tag:
== Adding a tag:
  $ git tag tag_name
  $ git tag -l
      Should show your new tag.
  $ git push origin --tags

== Removing a tag:

  $ git tag -d tag_name
  $ git push origin :refs/tags/tag_name

